import React from 'react'

export default function Channel() {
    return (
        <>
            channel
            {/* <div onClick={this.channelClick} siteId={props.siteId}></div>

            {this.state.channelClicked ?
                    <ChatContainer token={this.props.token}>Chat Container</ChatContainer>
                :
                null}    */}
        </>
    )
}
